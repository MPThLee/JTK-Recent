name: Lint Result

on: [push, pull_request]

jobs:
  lint:
    name: Run ESLint.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - uses: volta-cli/action@v1
      - name: Install Toolchain via volta
        run: bash .github/_initVolta.sh
      - name: NPM CI
        run: npm ci --no-audit
      - name: Run lint
        run: npm run lint